<%= link_to 'End match and return to homepage', game_path(@game), method: :delete %>
<br />

<%= current_user.email %>
<%= "The current_user id is " + current_user.id %>
<%= "The patcher id is " + @game.patcher_id %>

<%= react_component("Game", {id: @game.id, user_id: current_user.id}) %>
